# Website Design Example

This is a learning/practice project for website design recreation.

All design rules, workflow, and technical defaults are now handled by the
`design-website` skill (`~/.claude/skills/design-website/SKILL.md`).

Use `/design-website` to invoke.
